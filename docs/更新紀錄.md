# TanChin-Time-Clock-SQLite3 更新紀錄

這份文件用來保存每次功能優化、錯誤修正、資料結構調整、操作流程變更與備份流程異動。之後每次完成修改後，都要在這裡追加一筆紀錄，並且每次核心資產備份都會把這份文件一起打包。

## 備份指令

在專案根目錄執行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File ".\scripts\backup-core-assets.ps1"
```

預設備份輸出位置：

```text
C:\Users\nickt\OneDrive\Developer 公司系統\TanChin-Time-Clock-SQLite3 原始資料
```

備份內容包含：

- 專案原始碼與設定：主程式、瀏覽器端、桌面端、資料庫模組、匯出模組、package 設定。
- 視覺與介面資產：assets、icon、modals、browser-client、renderer-scripts。
- 開發文件與文案：docs、根目錄 txt/md 文件、研發提示語等。
- 備份工具本身：scripts。
- 營運核心資料：`%APPDATA%\TanChin-Time-Clock-SQLite3\app_data.db` 與相關 SQLite 檔。
- 使用者自訂素材：CustomSounds、ThemeImages、AuditArchives、logs。

不包含：

- `node_modules`
- `dist`
- Electron/Chromium 快取資料，例如 Cache、Code Cache、GPUCache、Network、Session Storage。

## 2026-05-28 / v1.7.0

### 新增：加班申請、主管審核與出勤查核
- 新增加班獨立資料表與申請流程，不混入請假資料；員工本人申請會送主管審核，主管替直屬員工申請會自動核准，主管替自己申請仍需上層主管審核。
- 員工工作台新增「加班申請」與「我的加班紀錄」，可查看本人、代申請與待審核的加班單。
- 管理者工作台新增「加班管理」，可檢視加班申請紀錄、待主管審核數與加班出勤查核警示。
- 新增加班出勤查核：列出「已核准加班但區間內沒有出勤打卡」與「疑似加班出勤但沒有核准加班單」兩類警示；此查核只供比對與人工檢視，不會直接修改出勤時間。
- 新增加班申請紀錄與加班出勤查核 CSV 匯出，方便管理者人工比對。
- 確認目前請假流程已會把核准請假帶入考勤報表與薪資請假明細，但尚未建立「核准請假 vs 實際打卡」的專屬查核警示清單。
- 更新 `browser-client/index.html` 的 JS cache busting，避免瀏覽器沿用舊版工作台程式。
- 版號由 `v1.6.5` 更新為 `v1.7.0`，同步更新 `package.json`、`package-lock.json` 與版本參考檔。
- 已完成核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260528-155644.zip`。

## 2026-05-21 / v1.6.5

### 修正：管理者讀卡註冊改由打卡失敗稽核補抓卡號
- 修正管理者工作台新增員工時，讀卡器已被打卡入口偵測並寫入打卡失敗稽核，但「卡片註冊 / 讀卡測試」欄位沒有填入卡號的情境。
- 新增管理者 API `/api/browser/admin/card-reader-capture/latest`，可從啟動讀卡後最新的打卡失敗稽核紀錄帶回完整卡號。
- 網頁端讀卡測試啟動後會同時等待欄位鍵盤輸入與後端稽核 fallback；若卡號被打卡入口先讀到，仍會自動填入員工資料的「卡號」欄位。
- 新增「帶入最近刷卡失敗」按鈕，供現場在讀卡器焦點被桌面打卡入口接走時手動帶回最近刷到的卡號。
- 更新 `browser-client/index.html` 的 JS cache busting，避免瀏覽器沿用舊版 `app.js`。
- 版號由 `v1.6.4` 更新為 `v1.6.5`，同步更新 `package.json` 與 `package-lock.json`。
- 本次核心資產備份檔：`TanChin-Time-Clock-SQLite3-core-assets-20260521-160632.zip`。

## 2026-05-21 / v1.6.4

### 新增：第一次使用系統完整說明書
- 新增 `docs/第一次使用系統完整說明書.html`，依照目前系統程式碼、資料表、網頁工作台與既有文件整理第一次使用流程。
- 說明書以最高權限維運視角撰寫，清楚區分系統管理者與開發人員的權責。
- 文件涵蓋預設帳密、首次登入、帳號權限管理、管理者基礎設定、考勤與請假、ERP 匯出、自動化任務、完整資料庫備份還原與首次驗收清單。
- 文件內加入系統證據來源對照，標示依據 `server.js`、`database.js`、`browser-client/app.js`、`attendance-export.js`、既有部署還原文件與更新紀錄撰寫。
- 版號由 `v1.6.3` 更新為 `v1.6.4`，同步更新 `package.json` 與 `package-lock.json`。
- 本次核心資產備份檔：`TanChin-Time-Clock-SQLite3-core-assets-20260521-140009.zip`。

## 2026-05-19 / v1.6.3

### 調整：自動化任務備份選項語意
- 將自動化任務類型中的「完整備份」改名為「完整資料庫備份」，讓用途更清楚。
- 任務目標下拉選單排除 `完整資料庫備份` 目標，避免在匯出或清理類型中出現不適用的備份目標。
- 當任務類型選為「完整資料庫備份」時，任務目標欄位維持隱藏，後端會固定正規化為完整資料庫備份目標。
- 更新 `browser-client/index.html` 的 JS cache busting，確保瀏覽器載入新版自動化任務表單邏輯。
- 版號由 `v1.6.2` 更新為 `v1.6.3`，同步更新 `package.json` 與 `package-lock.json`。
- 已完成核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260519-121543.zip`。

## 2026-05-19 / v1.6.2

### 調整：版本號移至主視覺區塊
- 依照畫面註解，將網頁版版本號從工作台操作列移到最上方主視覺區塊內，維持小型、低調顯示。
- 將「智慧好夥伴」副標從主視覺區塊左上方移到主標題後方，形成同列標題資訊。
- 公開設定 API 現在會帶回 `appVersion`，登入前後都能在主視覺區塊顯示正確版本。
- 移除工作台導覽列中的版本徽章，避免與登入成功、身份、說明、登出等操作按鈕混在一起。
- 更新 `browser-client/index.html` 的 CSS/JS cache busting，確保瀏覽器載入主視覺區塊新版配置。
- 版號由 `v1.6.1` 更新為 `v1.6.2`，同步更新 `package.json` 與 `package-lock.json`。
- 已完成核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260519-115349.zip`。

## 2026-05-19 / v1.6.1

### 調整：網頁版頂部版本號顯示
- 在網頁版登入後的上方導覽列右側加入低調的小型版本號徽章，顯示目前應用程式版本，例如 `v1.6.1`。
- 後端 dashboard 回傳統一帶入 `appVersion`，員工、管理者、開發者與系統管理者工作台都能使用同一份版本來源。
- 前端版本徽章會在未取得版本時自動隱藏，避免影響既有登入流程與導覽列操作。
- 更新 `browser-client/index.html` 的 CSS/JS cache busting，確保瀏覽器載入新版樣式與腳本。
- 版號由 `v1.6.0` 更新為 `v1.6.1`，同步更新 `package.json` 與 `package-lock.json`。
- 已完成核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260519-110738.zip`。

## 2026-05-19 / v1.6.0

### 新增：完整資料庫自動備份與第二階段還原
- 新增完整資料庫備份核心模組 `database-backup.js`，以 SQLite 線上備份建立 `.db` 檔，並同步輸出 `.manifest.json`、檔案大小與 SHA256，方便日後查核與外部保存。
- 開發者工作台 > 自動化任務新增「完整備份」任務類型，可立即執行或排程每日、每週、每月完整資料庫備份；任務可沿用預設備份資料夾，也可指定任務專用資料夾。
- 開發者工作台 > 匯出設定新增「完整資料庫備份與還原」子項，集中管理備份資料夾、保留份數、立即完整備份、備份檔選擇與資料庫還原。
- 第二階段還原已實作保護流程：還原前會先建立 `db-emergency` 緊急備份，還原來源需通過 SQLite `quick_check` 與必要資料表檢查，並要求輸入「還原資料庫」確認文字才會覆蓋目前資料庫。
- 系統狀態與開發者資料集新增資料庫備份路徑、有效路徑、保留份數、最近一般備份與最近緊急備份摘要；一般備份與還原前緊急備份已分開計數與顯示。
- 新增開發者 API：`/api/browser/developer/database-backup/run`、`/api/browser/developer/database-restore/run`、`/api/browser/developer/database-backup-settings/save`、`/api/browser/developer/database-backup-directory/select`、`/api/browser/developer/database-restore-file/select`。
- 已完成實測：建立完整資料庫備份後，以同一份備份執行還原，並確認還原前緊急備份成功產生。
- 版本由 `v1.5.0` 升級到 `v1.6.0`，已同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260519-102002.zip`。

## 2026-05-19 / v1.5.0

### 新增：開發者全域主功能頁籤排序
- 新增 `workspace_nav_order` SQLite 資料表，用於保存各工作台角色的主功能頁籤全域排序。
- 新增開發者專用 API：`/api/browser/developer/workspace-nav-order/save` 與 `/api/browser/developer/workspace-nav-order/reset`。
- 權限採「只允許實際開發者工作台操作」，開發者不需要切換到管理者工作台；在開發者工作台的「系統設定 > 主頁籤排序」即可選擇管理者工作台並調整排序。
- 排序支援拖曳、上移、下移、儲存與還原預設，儲存後會套用到對應角色的上方主功能頁籤列。
- 後端會驗證角色與頁籤 id，缺漏的新頁籤會自動補回預設順序尾端，避免未來新增功能後頁籤消失。
- 儲存與還原排序會寫入稽核紀錄，並透過即時同步通知其他管理者或開發者頁面重新載入。
- 已實測開發者可儲存管理者工作台排序、管理者 dashboard 會取得自訂排序、還原預設後回到原始順序，非開發者狀態呼叫開發者排序 API 會被 `403` 擋下。
- 更新瀏覽器端 CSS/JS cache busting，避免舊樣式或舊程式快取。
- 版本由 `v1.4.2` 升級到 `v1.5.0`，已同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260519-071401.zip`。

## 2026-05-19 / v1.4.2

### 調整：左側子導覽欄寬縮短三分之一
- 將管理者與開發者工作台內部的左側子導覽欄，由原本 `210px–240px` 壓縮為 `140px–160px`，整體約縮短三分之一。
- 同步壓縮子導覽按鈕高度、左右內距、文字尺寸與「子導覽數量徽章」寬度，讓左側功能導覽密度更高。
- 將管理者工作台「員工名冊與欄位」改名為「員工名冊資料」。
- `107/107` 這類右側小標籤在介面命名為「子導覽數量徽章」，程式 class 為 `workspace-subnav-badge`。
- 更新瀏覽器端 CSS/JS cache busting，避免舊樣式快取造成寬度未刷新。
- 版本由 `v1.4.1` 升級到 `v1.4.2`，已同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260519-062625.zip`。

## 2026-05-19 / v1.4.1

### 調整：頁籤摘要卡壓縮為單列並改用說明彈窗
- 將管理者與開發者各主功能頁籤上方的摘要卡壓縮為單列顯示，只保留分類、標題、狀態徽章與必要操作按鈕。
- 原本直接顯示在摘要卡中的功能說明文字，改由同列新增的「說明」按鈕開啟彈窗查看。
- 「說明」彈窗共用既有工作台說明 modal，內容會依目前頁籤摘要卡自動帶入，避免每個頁籤重複寫獨立彈窗。
- 保留員工匯入、匯出等原本在摘要卡上的操作按鈕，並將「說明」按鈕放在同一列，降低頁籤進入後的垂直占用。
- 已用瀏覽器實測開發者與管理者頁籤摘要卡、說明彈窗、console 錯誤狀態與 390px 窄版版面。
- 版本由 `v1.4.0` 升級到 `v1.4.1`，已同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260519-061815.zip`。

## 2026-05-18 / v1.4.0

### 調整：管理者與開發者工作台改為左側子導覽
- 保留上方主功能選單不變，將管理者與開發者每個主功能內的細項拆成固定左側子導覽，右側只顯示目前選取的內容。
- 管理者工作台已套用人員資料、安全設定、班別設定、手動補登、考勤報表、請假管理、系統設定、響鈴設定與主題特效。
- 開發者工作台已套用自動化任務、自動化日誌、操作稽核紀錄、系統設定、匯出設定與系統狀態。
- 子導覽依工作性質分組，桌面版採左側 sticky 導覽，窄版改為上方橫向可捲動子導覽列，避免整頁上下滑動找區塊。
- 子導覽中的內容面板不再套用全域展開/收合控制，點選左側項目後內容會直接顯示在右側。
- 保留原有表單、匯入匯出、讀卡、身份切換、帳號權限、請假、主題與自動化事件處理；編輯按鈕會自動切到對應子項後填入表單。
- 新增缺少子面板的 console warning 與編輯切換錯誤保護，降低未來新增面板造成索引錯位時的排查成本。
- 更新 `browser-client/index.html` 的 CSS/JS cache busting 版本，避免瀏覽器沿用舊版工作台腳本。
- 已驗證 `browser-client/app.js`、`server.js` 語法，並用瀏覽器實測開發者與管理者工作台子導覽、390px 窄版版面與 console 錯誤狀態。
- 版本由 `v1.3.0` 升級到 `v1.4.0`，已同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260518-215641.zip`。

## 2026-05-18 / v1.3.0

### 新增：考勤報表補列已核准請假並支援薪資請假明細
- 將已核准請假轉為報表層的「請假考勤列」，不寫入原始打卡表，避免污染真實打卡資料。
- 請假會依日期拆成每日考勤事件，並保留假別、當日請假起訖、當日時數、假單編號與來源「請假模組」。
- 考勤報表查詢、手動報表匯出與自動化考勤匯出改用同一套含請假補列的資料生成邏輯，避免查詢與匯出結果不一致。
- 既有薪資/完整/分析等匯出模板維持原欄位順序；另新增「薪資含請假明細」模板，供薪資與 ERP 需要解析請假時數時使用。
- 後台考勤報表畫面新增請假區段與請假時數欄位，半小時、半天、整天與不同假別都能在同一份考勤資料中查核。
- 請假查詢改用日期區間交集，不再受原本單次 500 筆查詢上限影響，較適合月份或全部資料匯出。
- 更新 `browser-client/index.html` 的 CSS/JS cache busting 版本，避免瀏覽器沿用舊版工作台腳本。
- 已驗證 `server.js`、`database.js`、`attendance-export.js` 與 `browser-client/app.js` 語法，並執行請假匯出模板與 Electron Node 資料庫查詢 smoke test。
- 版本由 `v1.2.5` 升級到 `v1.3.0`，已同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260518-195616.zip`。

## 2026-05-18 / v1.2.5

### 調整：員工工作台改為按鈕式區塊切換
- 將員工工作台的「私人資訊」、「近 7 日打卡紀錄」、「遠端打卡安全」、「請假申請」、「我的請假紀錄」整理成同一列按鈕式入口。
- 預設只顯示按鈕列與提示文字，點擊項目後才展開對應內容；再次點擊同一項目可收合。
- 保留上方員工資料摘要與目前時間/打卡操作，讓常用打卡動作不被藏入切換區。
- 員工工作台切換內容不再套用通用卡片「展開/收合」控制，避免點了功能按鈕後還要再展開一次。
- 將請假申請與我的請假紀錄拆成兩個獨立入口；若登入者有主管待審資料，會放在「我的請假紀錄」區塊內一併顯示。
- 已用網頁端員工登入驗證五個按鈕切換、內容展開與無二次摺疊控制；測試登入產生的裝置綁定已清除。
- 版本由 `v1.2.4` 升級到 `v1.2.5`，已同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260518-100756.zip`。

## 2026-05-18 / v1.2.4

### 調整：網頁版整體面板密度與表格欄寬整理
- 收緊網頁版外層容器、hero、面板、卡片、統計卡、表單、按鈕、徽章、modal 與主題預覽區的 padding、gap、圓角與字級，降低整體留白，讓工作台資訊更集中。
- 調整 `data-table` 表格呈現方式，新增欄位量測流程，會依表頭、內容、輸入值、placeholder、select 選項與按鈕文字估算欄寬，並以 110% 作為實際欄寬。
- 表格量測完成後會建立 `colgroup`，讓欄位依內容寬度收斂；外框同步縮到表格內容寬度，避免欄位被容器硬撐開。
- 更新 `browser-client/index.html` 的 CSS/JS cache busting 版本，確保瀏覽器重新載入後取得新版密度樣式。
- 已驗證 `browser-client/app.js` 與 `server.js` 語法，並用網頁端實看開發者工作台、系統狀態 API 表格與 390px 窄版畫面，確認未出現橫向溢出。
- 版本由 `v1.2.3` 升級到 `v1.2.4`，已同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產備份：`TanChin-Time-Clock-SQLite3-core-assets-20260518-094839.zip`。

## 2026-05-18 / v1.2.3

### 調整：請假管理假別設定編輯方式

- 將請假管理的假別設定改為假別按鈕切換，點選假別後只在下方顯示該假別設定。
- 新增「新增假別」操作，按下後顯示空白假別設定面板，不再預設展開所有假別欄位。
- 提高假別設定欄位密度，第一列放假別代碼、假別名稱、單位與顯示順序，第二列放啟用、有薪、扣額度與需附件，第三列放說明與備註。
- 保留既有假別儲存 API 與資料格式，僅調整網頁端操作版面與切換互動。
- 版本由 `v1.2.2` 提升至 `v1.2.3`，同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產備份，備份檔案：`TanChin-Time-Clock-SQLite3-core-assets-20260518-082211.zip`。

## 2026-05-18 / v1.2.2

### 修正：網頁版面板文字對比與灰化誤判

- 調整瀏覽器版全域正常副文字、小標與狀態色，避免可用文字看起來像停用或失效。
- 降低頁面背景與卡片玻璃覆蓋層的白色沖淡效果，改善面板上端文字偏灰、往下滑才變鮮明的視覺落差。
- 補強卡片內容層級，確保卡片裝飾漸層留在內容後方，不干擾可讀性。
- 補上表單欄位 disabled 樣式，讓真正不可操作的欄位與正常說明文字有明確區隔。
- 版本由 `v1.2.1` 提升至 `v1.2.2`，同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產備份，備份檔案：`TanChin-Time-Clock-SQLite3-core-assets-20260518-073815.zip`。

## 2026-05-17 / v1.2.1

### 調整：帳號權限管理入口與清單展開方式

- 調整帳號權限管理權責，改由網頁端系統管理者與開發人員主導；管理者端不再顯示「帳號權限管理」功能頁。
- 後端同步移除管理者可持有的帳號權限管理權限碼，帳號權限儲存 API 僅允許系統管理者或開發人員操作。
- 調整「帳號權限管理」清單設計，預設只顯示員工摘要、角色摘要與搜尋欄，點選單一員工的「設定」才展開角色與管理者功能權限。
- 新增清單搜尋，可用工號、姓名、部門或角色快速縮小員工範圍，避免一打開就看到全部員工的完整設定欄位。
- 版本由 `v1.2.0` 提升至 `v1.2.1`，同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產與使用者資料備份：`TanChin-Time-Clock-SQLite3-core-assets-20260517-152638.zip`。

## 2026-05-17 / v1.2.0

### 新增：網頁端系統管理者與預設員工權限

- 新增網頁端「系統管理者」登入入口，使用獨立帳號密碼管理瀏覽器端帳號權限，不綁定員工資料，也不參與打卡、請假或桌面端流程。
- 調整瀏覽器端帳號權限預設值；未被指派的員工僅能登入員工頁面，管理者與開發人員頁面需由系統管理者或具權限帳號授權。
- 新增系統管理者工作台，集中顯示帳號權限管理，並可在網頁端更新系統管理者帳號與密碼。
- 放寬帳號權限儲存限制，允許所有員工維持員工權限，避免為了保留管理入口而強制指定某位員工為完整管理者或開發人員。
- 本次調整僅套用 `/browser/` 與 `/api/browser/...` 網頁端登入、Session 與權限檢查，不變更桌面端打卡與管理流程。
- 版本由 `v1.1.54` 提升至 `v1.2.0`，同步更新 `package.json` 與 `package-lock.json`。
- 已執行核心資產與使用者資料備份：`TanChin-Time-Clock-SQLite3-core-assets-20260517-094214.zip`。

## 2026-05-16 / v1.1.54

### 修正：開發者稽核顯示完整打卡輸入值

- 依開發者排查需求，打卡失敗稽核資料新增保存完整 `credential_input`，記錄實際感應到或輸入的卡號/密碼值。
- 調整網頁版開發人員工作台「操作稽核紀錄」的打卡失敗診斷，「卡號紀錄」直接顯示完整輸入值、輸入長度、後 4 碼與完整卡號雜湊。
- 同步調整桌面端打卡失敗與外部 API 打卡失敗稽核，未來新產生的失敗紀錄會保留完整輸入值供開發者排查。
- 調整讀卡測試稽核紀錄，保存完整讀卡輸入值並顯示完整卡號雜湊。
- 更新網頁版 `app.js` 快取版本字串，避免瀏覽器載入舊的前端程式。
- 將版本由 `v1.1.53` 提升至 `v1.1.54`，並同步更新 `package.json` 與 `package-lock.json`。
- 依規則重新執行核心資產備份，確保本次修改與更新紀錄已納入備份流程。

## 2026-05-15 / v1.1.53

### 修正：網頁版打卡失敗稽核卡號紀錄顯示

- 查證網頁版開發人員工作台「操作稽核紀錄」的打卡失敗診斷面板原本只顯示卡號雜湊前 16 碼，完整卡號雜湊需展開 JSON 才看得到。
- 調整打卡失敗診斷面板，直接顯示完整卡號紀錄摘要：輸入長度、卡號後 4 碼與完整卡號雜湊。
- 保持不顯示明文完整卡號；稽核資料仍以後 4 碼與雜湊供管理者追查。
- 更新網頁版 `app.js` 快取版本字串，避免瀏覽器載入舊的前端程式。
- 將版本由 `v1.1.52` 提升至 `v1.1.53`，並同步更新 `package.json` 與 `package-lock.json`。
- 依規則重新執行核心資產備份，確保本次修改與更新紀錄已納入備份流程。

## 2026-05-15 / v1.1.52

### 調整：桌面版打卡異常提示精簡化

- 精簡桌面版員工打卡異常畫面文字，員工端只顯示卡號末 4 碼、簡短狀態與代碼，不再顯示完整系統錯誤原因。
- 調整 `P001` 找不到卡號或密碼、`P003` 打卡紀錄儲存失敗、`P004` 重複打卡紀錄儲存失敗的畫面提示。
- 同步精簡 `P002` 一分鐘內重複打卡提示，保留已記錄狀態與代碼。
- 後端與稽核紀錄仍保留詳細失敗原因、輸入長度、卡號末 4 碼與卡號雜湊，方便管理者後續追查。
- 將版本由 `v1.1.51` 提升至 `v1.1.52`，並同步更新 `package.json` 與 `package-lock.json`。
- 依規則重新執行核心資產備份，確保本次修改與更新紀錄已納入備份流程。

## 2026-05-13 / v1.1.51

### 修正：管理者工作台讀卡輸入未自動填入卡號

- 修正管理者工作台新增員工資料時，卡片註冊 / 讀卡測試的「讀卡輸入」欄位無法直接啟動讀卡的問題。
- 「讀卡輸入」欄位現在可直接點選啟動，也保留「讀取卡片」按鈕啟動流程。
- 新增讀卡自動收尾機制；若讀卡器只輸出卡號、沒有送出 Enter 或 Tab，系統仍會自動填入上方「卡號」欄位。
- 將版本由 `v1.1.50` 提升至 `v1.1.51`，並同步更新 `package.json` 與 `package-lock.json`。
- 本次核心資產備份檔：`TanChin-Time-Clock-SQLite3-core-assets-20260513-084447.zip`。

## 2026-05-13 / v1.1.50

### 修正：重複啟動造成 3000 連接埠占用

- 新增 Electron 單一實例保護，第二次啟動時會喚回既有視窗，不再重複開啟伺服器。
- 改善本機瀏覽器伺服器啟動錯誤處理；若 3000 連接埠被其他程式占用，會顯示友善提示並結束程式。
- 先手動關閉舊的 TanChin Electron 背景程序，釋放 3000 連接埠與 Electron cache 鎖定。
- 將版本由 `v1.1.49` 提升至 `v1.1.50`，並同步更新 `package.json` 與 `package-lock.json`。
- 本次核心資產備份檔：`TanChin-Time-Clock-SQLite3-core-assets-20260513-074149.zip`。

## 2026-05-12 / v1.1.49

### 調整：GitHub 同步後的 package 版本與 Windows 檢查穩定性

- 將 `package.json` 內非必要中文說明調整為 ASCII 註記，避免 Windows PowerShell 預設編碼讀取時造成 JSON 檢查誤判。
- 將版本由 `v1.1.48` 提升至 `v1.1.49`，並同步更新 `package.json` 與 `package-lock.json`。
- 重新確認 `origin` 指向 `nicktim791113/TanChin-Time-Clock-SQLite3`，且 `git pull --ff-only` 可正常完成。
- 依規則重新執行核心資產備份，確保修正後的專案檔與文件已納入備份流程。

## 2026-05-12 / v1.1.48

### 維護：GitHub 同步與版本發布流程

- 驗證專案可先執行 `git pull --ff-only` 拉回 GitHub 最新狀態，再進行本機修改、提交與推播。
- 新增 `.gitignore`，排除 `node_modules`、打包輸出、SQLite 實際資料庫、log 與環境變數檔，降低誤上傳風險。
- 將專案版本提升至 `v1.1.48`，並同步更新 `package.json` 與 `package-lock.json`。
- 依照新工作規則執行核心資產備份、版本更新、更新紀錄、差異檢查、提交與 GitHub 推播流程。
- 本次核心資產備份檔：`TanChin-Time-Clock-SQLite3-core-assets-20260512-093047.zip`。

## 2026-05-12 / v1.1.47

### 新增：請假申請與雙層審核模組

- 新增請假資料結構：假別設定、部門主管審核路徑、請假申請主檔與審核步驟。
- 預設建立常用假別：事假、特休、病假、婚假、喪假、公假與其他。
- 假別保留可調整欄位，包含假別名稱、單位、是否啟用、是否有薪、是否扣額度、是否需要附件與排序。
- 員工前端新增請假申請表單，可選假別、起迄日期時間、請假時數與原因。
- 員工可查看自己的請假紀錄，並可撤回尚未管理部終審的申請。
- 若員工被設定為部門主管，員工登入後會看到「待我審核」區塊，可先行核准或駁回部門員工請假單。
- 管理者工作台新增「請假管理」分頁，主管核准後的請假單會進入管理部待複核，由管理者終審核准或駁回。
- 請假終審核准後才視為生效，狀態流程為：主管審核中 → 管理部複核中 → 已核准。

### 新增：請假設定與權限控管

- 管理者可在請假管理中維護假別設定，不需修改程式碼即可調整公司假別規則。
- 管理者可設定部門對應主管，也可用 `*` 建立預設主管路徑。
- 主管仍以員工身份登入，只能審核被設定到自己名下的請假單，不會取得管理者權限。
- 開發人員身份切換可用來測試員工、主管與管理者流程；稽核紀錄會保留操作角色。

### 優化：考勤報表整合

- 管理者考勤報表會納入已核准請假資料，來源顯示為「請假模組」。
- 報表摘要新增請假筆數，避免已核准請假被歸類為異常。
- CSV 匯出支援請假來源與請假類型，方便後續薪資或人資系統串接。

### 風險控管與後續預留

- 第一版先完成申請、主管審核、管理部終審與報表顯示，不先硬寫完整法規額度計算。
- 附件欄位與額度扣抵採資料結構預留，後續可加入病假證明、特休額度流水帳與封存策略。
- 請假申請會檢查同一員工是否已有重疊的待審或已核准請假，降低重複申請風險。
- 請假操作會寫入操作稽核，方便追查申請、撤回、主管審核與管理部終審紀錄。

## 2026-05-10 / v1.1.47

### 新增：開發人員身份切換

- 開發人員登入後可在開發人員工作台切換到管理者或指定員工頁面進行測試。
- 身份切換只允許在已登入開發人員狀態下使用；一般員工與管理者登入頁不接受系統密碼作為萬用登入密碼。
- 新增「開發人員身份切換」控制開關，放在開發人員工作台的系統設定區塊，可由系統密碼驗證後啟用或停用。
- 切換測試身份時保留原本開發人員登入狀態，並提供返回開發人員頁面的操作入口。

### 優化：開發人員工作台版面

- 將工作台標題調整為「開發者工作台」，登入狀態與身份資訊移到右上角狀態膠囊。
- 將立即任務、排程觀測、安全維護等說明內容集中到右上角「說明」按鈕內，降低主畫面資訊密度。
- 將日誌與密碼頁面拆分為「自動化日誌」、「操作稽核紀錄」與「系統設定」等頁面。
- 將變更系統密碼與開發人員身份切換整合到「系統設定」，並提高欄位密度。

### 優化：功能區塊收合與釘選

- 工作台各功能區塊標題旁新增展開/收合按鈕，進入頁面時預設收合。
- 展開/收合狀態在同一次登入操作中保留，登出再登入後才回到預設狀態。
- 新增圖釘按鈕，可設定每個區塊跨登入後預設展開或預設收合。
- 統一功能區標題列右側狀態、按鈕與展開/圖釘控制的位置。

### 優化：管理者工作台版面與說明

- 管理者工作台的人員與編號、作息與問候、主題與特效說明移到右上角「說明」按鈕內。
- 安全設定加入更完整說明，包含裝置綁定、GPS 要求、定位精度、綁定解除與風險紀錄查看。
- 員工 CSV 匯入/匯出邏輯加入右上角說明：匯出為完整員工名冊，匯入採同工號更新、同卡號更新原持卡員工、新工號且卡號不重複則新增。
- 將「匯入 CSV」與「匯出 CSV」按鈕移到「員工與名冊」區塊，放在員工筆數與圖釘控制之間。

### 新增：管理者儀表板卡片明細

- 管理者工作台上方七張統計卡片改為可點擊開啟明細彈窗。
- 「員工數量」可設定監測人員範圍：全部員工、在職員工、指定部門或手動選取員工。
- 「班別數量」顯示所有班別，並標示目前時間落在哪個班別。
- 「今日正常打卡」依監測範圍顯示已正常打卡員工與未打卡員工。
- 「今日異常打卡」顯示今日異常打卡紀錄與未成功寫入的打卡失敗稽核。
- 「響鈴排程」顯示啟用/全部排程數量，並顯示下一個響鈴排程名稱與時間。
- 「問候語」與「自訂主題」新增明細，方便查看問候語對象、主題排程與節日特效狀態。

### 風險控管與資料安全

- 管理者儀表板的監測人員範圍只影響前端儀表板統計與明細，不會修改員工名冊、打卡流程或考勤報表。
- 今日出勤狀態改以員工為單位呈現，避免原本以打卡紀錄筆數統計造成「一人多筆」的誤解。
- 異常打卡明細區分「已寫入打卡紀錄」與「失敗稽核」，避免管理者誤判資料是否已進入正式打卡紀錄。
- 後端新增管理者儀表板明細資料，前端只負責呈現與本機監測範圍設定，降低資料不一致風險。

## 2026-05-09

### 新增：核心資產備份流程

- 新增一鍵備份腳本：`scripts/backup-core-assets.ps1`。
- 備份檔會輸出到 OneDrive 指定資料夾，檔名包含時間戳。
- 備份檔內會包含 `docs/更新紀錄.md`，確保修改紀錄可隨核心資料一起帶走。
- 新增 `docs/快速部署還原說明.md`，備份檔會一起帶走快速還原與部署說明。
- 備份會產生 `backup-manifest.json`，記錄打包時間、專案來源、營運資料來源與包含項目。
- 已完成第一次實際打包驗證，確認 zip 內包含原始碼、更新紀錄、備份腳本、營運資料庫與自訂素材。

### 建立：固定更新紀錄文案

- 新增本文件作為每次修改後的更新紀錄。
- 之後每次功能優化、問題修正或打包流程調整，都要在本文件追加日期與內容摘要。

## 2026-05-05

### 優化：打卡失敗診斷與讀卡測試

- 桌面端打卡失敗 LOG 補上錯誤代碼、失敗原因、輸入長度、後 4 碼與 `card_hash`。
- 一般打卡失敗不保存完整卡號，只保存可比對但不直接外洩的雜湊值。
- 管理者工作台新增卡片註冊/讀卡測試，可讀取完整卡號並填入員工卡號欄位。
- 開發人員工作台新增打卡失敗診斷顯示與快捷篩選。
- 管理者工作台資訊卡調整為今日正常打卡與今日異常打卡，其中重複打卡不列入異常計算。
